% ********************************************* %
\subsection{Operating Theatre Scheduling Problem\label{sec:ot}}
% ********************************************* %

Following the proposed integrated framework, the final step consists in solving the Operating Theatre Scheduling Problem (OTSP), which addresses the tactical allocation of surgical subspecialties to operating theatre blocks. 
This stage translates the strategic decisions derived in the preceding modules into implementable schedules for hospital operations. 
In particular, the OTSP takes as input the target number of surgeries for each sub-speciality, as determined by the $(R,Q)$ policy, together with the effective block capacity obtained from the cancellation and overtime management module.

We formulate the OTSP as a Mixed-Integer Linear Programming (MILP) model. This formulation allows the explicit representation of operational constraints and multiple, potentially competing objectives, while ensuring optimality with respect to the specified criteria. 
The model is designed to reflect hospital priorities such as high operating theatre utilisation, the allocation of priority subspecialities, and the efficient sequencing of blocks from a logistical perspective.

The proposed framework is, however, not restricted to the MILP approach adopted here. 
Since the previous modules provide demand and effective capacity as structured inputs, alternative solution methods, including heuristics and metaheuristics, could also be employed in the OTSP stage.
The main value of the framework therefore lies in the integration of decision layers, rather than in the exclusive use of a particular scheduling technique.

In the following subsections, we first describe how the strategic outputs of the $(R,Q)$ policy and the effective capacity information are preprocessed into block-demand units, which constitute the relevant unit of the scheduling problem. 
We then present the MILP formulation of the OTSP, including its decision variables, objective functions, and constraints.

\subsubsection{Preprocessing Strategic Input\label{subsec:preprocess_otsp}}

The OTSP is formulated at the block level. 
Therefore, the strategic outputs of the previous modules must be converted into the number of blocks required for each subspecialty.

For each subspecialty $\specialtyidx \in \specialtyset$, let
\[
p_{\specialtyidx}(k)=P(\numcompletedsurgs = k), \qquad k=0,1,\dots,\optplannedsurgs_{\specialtyidx},
\]
denote the probability mass function of the number of surgeries that can be completed in a single block under the optimal planning decision obtained in the cancellation and overtime management module. 
This distribution summarises the effective stochastic capacity of one block for subspecialty $\specialtyidx$.

To determine the number of blocks required to achieve activity targets $\lowq$ and $\upq$ at confidence level $\conflevel$, we use successive convolutions of $p_{\specialtyidx}(\cdot)$. 
Let
\[
p_{\specialtyidx}^{(n)} = \underbrace{p_{\specialtyidx} \ast \cdots \ast p_{\specialtyidx}}_{n \text{ times}}
\]
denote the probability mass function of the total number of surgeries that can be completed across $n$ blocks allocated to subspecialty $\specialtyidx$, and let $F_{\specialtyidx}^{(n)}$ be its corresponding cumulative distribution function.

Then, for a generic target $\targetq \in \{\lowq,\upq\}$, the minimum number of blocks required is the smallest integer $n \in \mathbb{N}_{+}$ such that
\begin{equation}\label{eq:block_requirement}
P\!\left(\sum_{b=1}^{n} \numcompletedsurgs_b \geq \targetq\right)
=
1 - F_{\specialtyidx}^{(n)}(\targetq - 1)
\geq
\conflevel,
\end{equation}
where $\numcompletedsurgs_b$ denotes the number of surgeries completed in block $b$, assumed independent and identically distributed according to $p_{\specialtyidx}(\cdot)$.

In other words, we seek the smallest number of blocks such that the probability of completing at least $\targetq$ surgeries is no smaller than $\conflevel$. 
This procedure is applied separately to the lower and upper activity targets. Algorithm~\ref{alg:blockdemand} summarises the computation.

\begin{algorithm}[H]
\scriptsize{
\caption{Block demand estimation via successive convolutions\label{alg:blockdemand}}
\begin{algorithmic}[1]
\State \textbf{Input:} set of subspecialties $\specialtyset$, targets $(\lowq,\upq)$, confidence level $\conflevel$, PMF $p_{\specialtyidx}$ for each $\specialtyidx \in \specialtyset$
\State \textbf{Output:} block requirements $(\lowerblockdemand,\upperblockdemand)$ for each $\specialtyidx \in \specialtyset$

\For{each subspecialty $\specialtyidx \in \specialtyset$}
    \If{$\lowq = 0$ and $\upq = 0$}
        \State $\lowerblockdemand \gets 0$, $\upperblockdemand \gets 0$
    \Else
        \For{each $\targetq \in \{\lowq,\upq\}$}
            \State $n \gets 1$
            \State $p_{\specialtyidx}^{(n)} \gets p_{\specialtyidx}$
            \State $F_{\specialtyidx}^{(n)} \gets \calccdf\!\left(p_{\specialtyidx}^{(n)}\right)$
            \While{$1 - F_{\specialtyidx}^{(n)}(\targetq - 1) < \conflevel$}
                \State $p_{\specialtyidx}^{(n+1)} \gets \convolve\!\left(p_{\specialtyidx}^{(n)}, p_{\specialtyidx}\right)$
                \State $n \gets n + 1$
                \State $F_{\specialtyidx}^{(n)} \gets \calccdf\!\left(p_{\specialtyidx}^{(n)}\right)$
            \EndWhile
            \If{$\targetq = \lowq$}
                \State $\lowerblockdemand \gets n$
            \Else
                \State $\upperblockdemand \gets n$
            \EndIf
        \EndFor
    \EndIf
\EndFor
\end{algorithmic}
}
\end{algorithm}

This preprocessing step is essential because the OTSP is defined over block allocations rather than individual surgeries. 
It also provides a mechanism for propagating the uncertainty captured in the strategic layer into the tactical scheduling stage without explicitly formulating the OTSP as a stochastic optimisation model. 
As a result, the scheduling problem can be solved as a deterministic MILP while remaining consistent with surgery allocation targets that reflect both variability in surgery durations and the effective capacity of each block.

\subsubsection{MILP Formulation}

Modeling the OTSP as a MILP allows us to capture the complex interactions between subspeciality demand, block capacity, and scheduling constraints in a structured manner.
To formulate the problem, we use the the notation in Table~\ref{tab:symb}.
\begin{table}[t!]
    \centering
    \caption{\label{tab:symb}Table of Symbols}
    \scriptsize
    \begin{tabular}{cl}
    \toprule
    \textbf{Set}                    & \textbf{Description}                                                                       \\
    \midrule
        $\roomset$                       & Operating theatres (both elective and outpatient)                       \\
        $\planninghorizonset$            & Days of the planning horizon, such that $\planninghorizonset = \{1, 2, ..., \days\}$  \\
        $\timeperiodset$                 & Periods (morning or afternoon), such that $\timeperiodset = \{\text{morning}, \text{afternoon}\}$          \\
        $\blockset$                      & Blocks, such that $\blockset = \planninghorizonset \times \timeperiodset$     \\
        $\specialtyset$                  & Surgical subspecialties                                                                \\
        $\specialtymset$                 & Surgical subspecialties that necessarily require two sequential allocated blocks,\\
                                         & such that $\specialtymset \subset \specialtyset$          \\
        % $\specialtycset$                 & Occasional surgical subspecialties (do not occur every month),                      \\
                                         & but have high priority, such that $\specialtycset \subset \specialtyset$              \\
        $\specialtyprset$                & Priority surgical subspecialties, such that $\specialtyprset \subset \specialtyset$  \\
        $\specialtytxset$                & Organ transplant surgical subspecialties, such that $\specialtytxset \subset \specialtyset$                                                                                                              \\
    \midrule
    \textbf{Index}                           & \textbf{Description}                                                                  \\
    \midrule
        $\roomidx$                            & Operating theatre, with $\roomidx \in \roomset$                                       \\
        $\planninghorizonidx$                 & Planning horizon day, with $\planninghorizonidx \in \planninghorizonset$ \\
        $\timeperiodidx$                      & Period (morning or afternoon), with $\timeperiodidx \in \timeperiodset$           \\
        $\blockidx$                           & Surgery block, with $\blockidx \in \blockset$                                    \\
        $\specialtyidx$                       & Surgical subspecialty, with $\specialtyidx \in \specialtyset$                      \\
    \midrule
    \textbf{Parameter}                        & \textbf{Description}                                                                  \\
    \midrule
        $\upperblockdemand$                                         & Maximum number of blocks allocated to each subspecialty $\specialtyidx \in \specialtyset$            \\
        $\lowerblockdemand$                                         & Minimum number of blocks allocated to each subspecialty $\specialtyidx \in \specialtyset$            \\
        $\specteams_{\blockidx\specialtyidx}$              & Number of teams of subspecialty $\specialtyidx \in \specialtyset$ available in block  $\blockidx \in \blockset$ \\
        $\numbersanests_{\blockidx}$                       & Number of anesthesiologists available in block $\blockidx \in \blockset$                         \\
        $\needanest_{\specialtyidx}$                       & 1 if subspecialty $\specialtyidx \in \specialtyset$ requires an anesthesiologist; 0 otherwise         \\    
        % $\mindeficit_{\specialtyidx}$                      & Minimum number of blocks for subspecialty $\specialtyidx \in \specialtyset$            \\
        $\anestconstant$                                   & Constant to penalize the anesthesiologist slack variable $\anestslackvar$ in the objective function ($\anestconstant = |\blockset|\cdot|\specialtyset|\cdot|\roomset|$) \\
        $\allowedrooms_{\roomidx\blockidx\specialtyidx}$   & 1 if room $\roomidx \in \roomset$, in block $\blockidx \in \blockset$, can              \\
                                                           & be used by subspecialty $\specialtyidx \in \specialtyset$ in the shift block $\blockidx \in \blockset$; 0 otherwise          \\
    \bottomrule
    \textbf{Variable}                         & \textbf{Description}                                                                  \\
    \midrule
        $\blockvar$                                        & 1 if room $\roomidx \in \roomset$, in block $\blockidx \in \blockset$, is allocated to subspecialty $\specialtyidx \in \specialtyset$; 0 otherwise. \\
                                                           & This variable is only created if the allocation is feasible (i.e., specialty $\specialtyidx$ is compatible with the theatre $\roomidx$).                \\
        $\precvar$                                         & 1 when room $\roomidx \in \roomset$, on day $\planninghorizonidx \in \planninghorizonset$, is allocated only to subspecialty $\specialtyidx \in \specialtyset$; 0 otherwise  \\
        $\integervar$                                      & Number of transplants to be performed in block $\blockidx \in \blockset$ by subspecialty $\specialtyidx \in \specialtyset$ \\
        % $\balancevar$                                      & Minimum allocation proportion by demand among all subspecialties   \\
        $\anestslackvar$                                   & Slack variable for the number of anesthesiologists available in block $\blockidx \in \blockset$ \\
        $\teamslackvar$                                    & Slack variable for the number of teams available in block $\blockidx \in \blockset$ for subspecialty $\specialtyidx \in \specialtyset$ \\
        \bottomrule
    \end{tabular}
\end{table}

The Objective Functions (OFs) are organised according to the priority levels established by the hospital's guidelines -- although they can be reordered as needed to reflect different institutional priorities in other settings.
To reflect these priorities, we adopt a hierarchical optimisation strategy, as proposed by~\citet{Aringhieri2022Hierarchical}.
The OFs are optimised sequentially, while respecting the optimal values obtained in the previous stages.

Formally, the first OF is defined as
\begin{align}
    \text{Min } \displaystyle F_0 = \sum_{\blockidx \in \blockset} \sum_{\specialtyidx \in \specialtyset} \teamslackvar + \anestconstant \cdot \anestslackvar, \label{eq:f0}
\end{align}
which minimises the total slack in team and anesthesiologist availability, with a high penalty on the latter as they are more scarce and expensive.
The second function is given by
\begin{align}
    \text{Min } \displaystyle F_1 = \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \sum_{\specialtyidx \in \specialtyset} \blockvar  \text{ only for variables where } \allowedrooms_{\roomidx\blockidx\specialtyidx} = 0 \label{eq:f1}
\end{align}
which minimises the number of allocations outside the aspirational theatre allocation defined for each subspecialty, thus promoting the use of preferred theatres.
The third objective function is defined as
\begin{align}
    \text{Max } \displaystyle F_2 = \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \sum_{\specialtyidx \in \specialtyprset} \blockvar, \label{eq:f2}
\end{align}
which maximises the allocation of priority subspecialties, ensuring that they receive as much capacity as possible within the constraints of the system.
The fourth OF is given by
\begin{align}
\text{Max } \displaystyle F_3 = \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \sum_{\specialtyidx \in \specialtyset} \blockvar, \label{eq:f3}
\end{align}
which maximises the total number of allocated blocks, thus promoting overall capacity utilisation and service provision.
Finally, the fifth OF is defined as
\begin{align}
    \text{Max } \displaystyle F_4 = \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \sum_{\specialtyidx \in \specialtyset} \blockvar \label{eq:f4}
\end{align}
which maximises the number of sequentially allocated blocks.
This is particularly important for improving the logistics of the surgical process, reducing the need for equipment and team transfers between theatres.

With this, we formulate the problem, incorporating these OFs and constraints, but with the flexibility to accommodate other settings as needed.

\begin{alignat}{4}
    (\modelname) \quad & \omit\rlap{Optimize $F_0$--$F_5$}\nonumber\\
    & \mbox{subject to} && \quad & \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \blockvar  & \leq \upperblockdemand                    &\qquad & \forall\, \specialtyidx \in \specialtyset  \label{eq:c1}\\
    &                  && \quad & \sum_{\roomidx \in \roomset} \sum_{\blockidx \in \blockset} \blockvar  & \geq \lowerblockdemand   &\qquad & \forall\, \specialtyidx \in \specialtyset \label{eq:c2}\\
    &                  && \quad & \sum_{\specialtyidx \in \specialtyset} \blockvar                       & \leq 1     &\qquad & \forall\, \roomidx \in \roomset,\, \blockidx \in \blockset \label{eq:c3}\\
    &                  && \quad & \sum_{\roomidx \in \roomset} \sum_{\specialtyidx \in \specialtyset} \needanest_{\specialtyidx} \cdot \blockvar   & \leq \numbersanests_{\blockidx} + \anestslackvar &\qquad & \forall\, \blockidx \in \blockset \label{eq:c4}\\
    &                  && \quad & \sum_{\roomidx \in \roomset} \blockvar   & \leq \specteams_{\blockidx\specialtyidx} + \teamslackvar  &\qquad & \forall\, \,\blockidx \in \blockset,\,\specialtyidx \in \specialtyset \label{eq:c5}\\
    &                  && \quad & \blockvar + x_{\roomidx(\blockidx+1)\specialtyidx} - 1   & \leq \precvar   &\qquad & \forall\, \roomidx \in \roomset,\, \blockidx \in \blockset,\,\specialtyidx \in \specialtyset, \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \planninghorizonidx \in \planninghorizonset :\,\text{ $\blockidx$ and $\blockidx+1$ are blocks} \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \text{ of the same day $\planninghorizonidx$}\label{eq:c6}\\
    &                  && \quad & \blockvar + x_{\roomidx(\blockidx+1)\specialtyidx}       & \geq 2 \cdot \precvar   &\qquad & \forall\, \roomidx \in \roomset,\, \blockidx \in \blockset,\,\specialtyidx \in \specialtyset, \planninghorizonidx \in \planninghorizonset : \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \planninghorizonidx \in \planninghorizonset : \text{ $\blockidx$ and $\blockidx+1$ are blocks} \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \text{ of the same day $\planninghorizonidx$}\label{eq:c7}\\
    &                  && \quad & \blockvar       & = x_{\roomidx(\blockidx+1)\specialtyidx}   &\qquad & \forall\, \roomidx \in \roomset,\, \blockidx \in \blockmset,\,\specialtyidx \in \specialtymset,\, \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \planninghorizonidx \in \planninghorizonset : \text{ $\blockidx$ and $\blockidx+1$ are blocks} \nonumber\\
    &                  &&       &                                                          &                 &\qquad & \text{ of the same day $\planninghorizonidx$}\label{eq:c8}\\
    &                  && \quad & \sum_{\roomidx \in \roomset} \blockvar   & = 2 \cdot \integervar  & \qquad & \forall\, \blockidx \in \blockset,\,\specialtyidx \in \specialtytxset  \label{eq:c9}\\
    &                  &&       & \blockvar                           & \in \{0,1\}                 &        & \forall\, \roomidx \in \roomset,\, \blockidx \in \blockset,\, \specialtyidx \in \specialtyset\nonumber \\
    &                  &&       & \precvar                            & \in \{0,1\}                 &        & \forall\, \roomidx \in \roomset,\, \planninghorizonidx \in \planninghorizonset,\, \specialtyidx \in \specialtyset\nonumber \\
    &                  &&       & \integervar                         & \in \mathbb{Z_{+}}          &        & \forall\, \blockidx \in \blockset,\, \specialtyidx \in \specialtyset\nonumber\\
    &                  &&       & \anestslackvar                      & \in \mathbb{Z_{+}}          &        & \forall \, \blockidx \in \blockset\nonumber\\
    &                  &&       & \teamslackvar                       & \in \mathbb{Z_{+}}.          &        & \forall \, \blockidx \in \blockset,\, \specialtyidx \in \specialtyset \nonumber
\end{alignat}

\bigskip

Constraints~\eqref{eq:c1} and~\eqref{eq:c2} impose minimum and maximum bounds on the number of allocations of each subspecialty.
This is part of the integration with the strategic layer, as these bounds are integrated to $(R,Q)$ policy and designed to enforce a prescribed waiting time requirement subspeciality, while taking full consideration of the uncertainty in surgery durations and the associated risk of surgery cancellations. % which in turn is influenced by the $(R,Q)$ policy.
Constraint~\eqref{eq:c3} ensures that each theatre and time block receives at most one subspeciality.
Constraint~\eqref{eq:c4} is a soft constraint that limits the number of allocations that require anaesthetists according to their availability $\numbersanests_{\blockidx}$ in each block, while allowing for some flexibility through the slack variable $\anestslackvar$.
Constraint~\eqref{eq:c5} similarly limits the number of allocations for each subspecialty based on the availability of medical teams $\specteams_{\blockidx\specialtyidx}$, with the slack variable $\teamslackvar$ providing flexibility when necessary.
Constraints~\eqref{eq:c6} and~\eqref{eq:c7} together guarantee sequential allocation of blocks on the same day when $\precvar = 1$.
Subspecialities requiring two consecutive blocks are specifically addressed by constraint~\eqref{eq:c8}.
Constraint~\eqref{eq:c9} ensures that transplant subspecialties are allocated in pairs, reflecting the need for simultaneous operating theatre use for donor and recipient procedures.tion balancing in conjunction with OF~\eqref{eq:f2}.