% ********************************************
\section{Estimating the number of patients per time block}
% *******************************************

To ensure consistency with the waiting-time requirements established by the $(R,Q)$ policy in Section~\ref{sec:waitinglist}, it is necessary to estimate how many elective patients can be accommodated within a single $6$-hour operating theatre block, in accordance with the scheduling regulations at HC-Unicamp. 
This estimation determines whether the batch size $Q$ defined at the strategic level can be effectively served within the planning horizon.

Accurate estimation requires explicit consideration of uncertainty in surgery durations, which varies across subspecialties and directly affects both operating theatre utilisation and the risk of overtime. 
In particular, a trade-off arises between idle time, resulting from underutilised theatre capacity, and overtime, which may trigger cancellations of scheduled procedures. 

For this purpose, in this section we present the second step of the proposed integrated framework. 
A newsvendor-based model is first employed to determine an optimal time allocation for the final surgery in a block under duration uncertainty, balancing idle-time and overtime costs. 
Building on this result, an inventory-based formulation is then used to estimate the number of patients that can be reliably scheduled within a time block, providing the tactical input required for subsequent operating theatre allocation decisions.

\subsection{Managing surgery cancellations via a newsvendor-based model\label{sec:cancel}}

Let us now consider the problem of determining the optimal time budget for the last surgery in a session, based on the random duration of this surgery. Let $T_D$ be a positive random variable taking values from $\Omega \in \mathbb R_+$, which represents the duration of a given surgery, with cumulative distribution $F_{T_D}: \Omega \to [0,1]$. Assume that, once the theatre is ready for the surgery, it only proceeds as planned if the remaining time in the session exceeds a prescribed budget of $t_b \in \Omega$ time units. Otherwise, the surgery is called off.

The time budget $t_b \in \Omega$ can be interpreted as a perishable inventory of session time. If the inventory is excessive, we will not fully utilise the time allocated to the surgical team to perform the surgery. Conversely, if we proceed with the surgery, but the time budget is insufficient, the surgical team will have to work overtime to complete the surgery. Let $c>0$ be the cost of each unit of time budget in excess of the actual surgical time $T_D$ and $p > c$ be a penalty for each unit of overtime that the surgical team incurs. The expected cost of the time deviation between $T_D$ and $t_b$ is defined as: 
%
\begin{equation} \label{eq:newsvendor}
u( t_b ) = c E (\beta_1) + p E( \beta_2 ), 
\end{equation}
%
where $\beta_1 = \min\{ t_b - T_D, \; 0 \}$ is the underused time budget, and $\beta_2 = \max \{ T_D - t_b, \; 0 \}$ is the total overtime. The objective is to find an optimal time budget allocation $t^*$ that minimizes the time deviation cost in Eq. \eqref{eq:newsvendor}, which is given by:
%
\begin{equation} \label{eq:mintimebudget}
    t^* =  \arg \min_{t_b \in \Omega} u( t_b ).
\end{equation}

\begin{lemma}
    Let $\beta_1 = \min\{ t_b - T_D, \; 0 \}$ and $\beta_2 = \max \{ T_D - t_b, \; 0 \}$. Then, it follows that:
\begin{equation} \label{eq:beta}
 E(\beta_1 ) =  \int_0^{t_b} F_{T_D}(t) dt, \quad    E(\beta_2 ) = \int_{t_b}^\infty (1 - F_{T_D}(t) ) dt
\end{equation}
\end{lemma}
%
\begin{proof}
We will start by proving the result for $\beta_1$. Since $\beta_1 \ge 0$, it follows that:
\begin{align*}
E(\beta_1) = \int_{t=0}^\infty P ( \beta_1 > t ) dt = \int_{t=0}^{t_{b}} P( t_b - T_D > t ) dt \\
E(\beta_1) = \int_{t=0}^{t_{b}} P( T_D < t_b - t ) dt = \int_{t=0}^{t_{b}} F_{T_D}(t_b - t) dt \\
E(\beta_1) = \int_{t=0}^{t_{b}} F_{T_D}(t) dt.
\end{align*}
%

Since $\beta_2$ is also non-negative, we can write:
%
\begin{align*}
    E(\beta_2) =  \int_0^\infty P( T_D - t_b > t) dt = \int_0^\infty P( T_D  > t + t_b) dt \\
    E(\beta_2) = \int_{t_b}^\infty P( T_D  > t ) dt = \int_{t_b}^\infty (1 - F_{T_D}(t) ) dt
\end{align*}
\end{proof}

\begin{theo} \label{thm:quantile}
Let $u(t_b)$ be defined in Eq. \eqref{eq:newsvendor}, and $t*$ be a solution to Eq. \eqref{eq:mintimebudget}. Then, if follows that:
%
\begin{equation} \label{eq:optimalquantile}
t^* = F_{t_D}^{-1} \left( \dfrac{ \dfrac{p}{c}}{1 + \dfrac{p}{c}}\right),
\end{equation}
%
where $F_{T_D}^{-1}(\cdot)$ is the inverse of the cumulative distribution function $F_{T_D}(\cdot)$.
\end{theo}
%
\begin{proof}
    Substituting \eqref{eq:beta} in Eq. \eqref{eq:newsvendor} yields:
\[
u( t_b ) = c  \int_0^{t_b} F_{T_D}(t) dt + p \int_{t_b}^\infty (1 - F_{T_D}(t) ) dt.
\]
% 
When $t_b = t^*$, the first order optimality condition for $u(t_b)$ implies:
%
\[
\dfrac{d u( t_b )}{d t_b} = c F_{T_D}(t^*) - p ( 1 - F_{T_D}(t^*) ) = 0.
\]
%
Hence, we must have:
%
\[
c F_{T_D}(t^*) - p ( 1 - F_{T_D}(t^*) ) \implies \dfrac{F_{T_D}(t^*)}{( 1 - F_{T_D}(t^*) )} = \dfrac{p}{c} \implies F_{T_D}(t^*) \left( 1 + \dfrac{p}{c} \right) = \dfrac{p}{c}
\]
% 
We conclude the proof by noting that \eqref{eq:optimalquantile} follows from the expression above.
\end{proof}

Theorem \ref{thm:quantile} implies that, whenever the remaining time in a session exceeds the quantile $t^*$ in Eq. \eqref{eq:optimalquantile}, the surgery is greenlighted to continue. Otherwise, the surgery is cancelled and the session comes to a close. Note that the quantile is solely a function of the ratio between the overtime cost $p$ and the cost of unused session time $c$, and is applicable to any surgery time distribution. For example, if $p = 2c$, $t^* = F^{-1}_{T_D} \left( \dfrac{2}{3} \right)$. This means that at least two thirds of the green-lighted surgeries will not require overtime.

% ******************************************************************************************** %
\subsection{Applying the newsvendor model to the case study\label{sec:applicationnewsvendor}}
% ******************************************************************************************** %


\textcolor{blue}{preciso sincronizar os símbolos matematicos...}

Building on the theoretical results of the previous subsection, here we address the following question: what is the probability that $k+1$ surgeries can be accommodated within an operating theatre block of length $T_t$?
Equivalently, this corresponds to the probability that the remaining time after completing $k$ surgeries exceeds the time budget threshold $t^{*}$. 
This probability can be evaluated through convolutions of the surgery-duration distributions.

Let $d_i$ denote the random duration of the $i$-th surgery and let $s_k=\sum_{i=1}^{k} d_i$ represent the cumulative duration of the first $k$ surgeries. The probability that sufficient time remains to schedule an additional surgery is then given by
\begin{align*}
P(T - s_k > t^{*}) &= P(s_k < T - t^{*}) \\
                  &= F_{s_k}(T - t^{*}),
\end{align*}
where $F_{s_k}$ denotes the cumulative distribution function of $s_k$. 
Assuming that at least one surgery always fits within the block (i.e., \textcolor{blue}{$F_{s_0}(t) = 1, \forall t \ge 0$}), the probability that $k+1$ surgeries can be scheduled in a block of length $T$ is therefore $P(s_k < T - t^{*})$.

Based on this formulation, we define $K_{\max}$ as the maximum number of surgeries that can be reliably accommodated within a single block. 
Specifically, \textcolor{red}{$K_{\max} = \{ \max k > 0: Fs_k (T - t^{*}) > \varepsilon \}$}. \textcolor{red}{Joao, isso eh o numero de cirurgias acomodadas, ou o numero de cirurgias acomodadas menos 1? Pense direito com as definicoes.}
%is determined as the largest integer $k$ such that \textcolor{red}{$Fs_k (T - t^{*}) > \varepsilon$}, where $\varepsilon$ is a prescribed minimum probability threshold. 

Given the model parameters $(T_t, t^{*}, K_{\max})$ and the discretised probability mass function of surgery durations, the probabilities that $k+1$ surgeries fit within the block can be computed for all $k \in \{0,\ldots,K_{\max}\}$ using the algorithm described below. \textcolor{red}{Note that here you say that you will accommodate $k+1$ surgeries, which contradicts the definition of that $K_{\max}$ is the maximum number of surgeries. Do we really need a definition in words for $K_{\max}$. Perhaps we can just defined $K_{\max}$ mathematically and deferring the definition of the number of surgeries to schedule until later on.}

\textcolor{blue}{corrigir latex do algoritmo}

% \begin{algorithm}[H]
% \caption{Calculating probabilities via convolutions and the newsvendor model}
% \begin{algorithmic}[1]
% \State \textbf{Input:} $prob\_vector$, $\epsilon$
% \State \textbf{Output:} $probs\_of\_fitting$
% \State Initialize $probs\_of\_fitting$ and $cumulative\_conv$ as empty vectors
% \State $probs\_of\_fitting[0] \gets 1$ \Comment{The first surgery always fits}
% \State $k \gets 0$
% \While{$probs\_of\_fitting[k] > \epsilon$}
%     \If{$k = 0$}
%         \State $cumulative\_conv[k] \gets prob\_vector$
%     \Else
%         \State $cumulative\_conv[k] \gets \text{Convolve}(cumulative\_conv[k-1],\, prob\_vector)$
%     \EndIf
%     \State $probs\_of\_fitting[k+1] \gets \text{RunNewsvendor}(cumulative\_conv[k],\, prob\_vector, k)$
%     \State $k \gets k + 1$
% \EndWhile
% \end{algorithmic}
% \end{algorithm}

The function \texttt{Convolve} (line $10$) computes the convolution between the cumulative distribution of the first $k$ surgeries ($cumulative\_conv[k-1]$) and the distribution of a single surgery ($prob\_vector$).
The function \texttt{RunNewsvendor} (line $12$) calculates the probability that $k+1$ surgeries fit within the block ($probs\_of\_fitting[k+1]$ on line $12$), given the cumulative convolution vector $cumulative\_conv[k]$ and the model parameters.
This way, the algorithm iteratively computes the probabilities until the threshold $\epsilon$ is no longer satisfied.

The vector $probs\_of\_fitting[k]$ will be filled with the probabilities of performing $X > k$ surgeries in the block, that is:
\begin{align*}
    &\text{For k = 0, } &probs\_of\_fitting[0] &= P(0 < T - t^{*}) = P(X > 0)\\
    &\text{For k = 1, } &probs\_of\_fitting[1] &= P(s_1 < T - t^{*}) = P(X > 1)\\
    &\text{For k = 2, } &probs\_of\_fitting[2] &= P(s_2 < T - t^{*}) = P(X > 2)\\
    &\text{For k = 3, } &probs\_of\_fitting[3] &= P(s_3 < T - t^{*}) = P(X > 3)\\
    & &\vdots &\\
    &\text{For k = $K_{max}$, } &probs\_of\_fitting[K_{max}] &= P(s_{K_{max}} < T - t^{*}) = P(X > K_{max}).\\
\end{align*}

\textcolor{red}{$X$ is not defined.}
To calculate the probability that $k$, $k > 0$ surgeries will occur in the block, we can simply calculate the complement
\begin{align*}
    P(X = k) &= P(X > k - 1) - P(X > k),
\end{align*}
thus, following the previous example, we have that:
\begin{align*}
    &\text{For k = 1, } &P(X = 1) &= P(X > 0) - P(X > 1) &\text{(probability of 1 surgery in the block)}\\
    &\text{For k = 2, } &P(X = 2) & = P(X > 1) - P(X > 2) &\text{(probability of 2 surgeries in the block)}\\
    &\text{For k = 3, } &P(X = 3) &= P(X > 2) - P(X > 3) &\text{(probability of 3 surgeries in the block)}\\
    & &\vdots & &\\
    &\text{For k = $K_{max}$, } &P(X = K_{max}) &= P(X > K_{max} - 1) - P(X > K_{max}) &\text{(probability of $K_{max}$ surgeries in the block).}\\
\end{align*}

From the probabilities $P(X = k)$, we can obtain the probabilities of cancellations as follows.
Let $n$ be the number of surgeries planned in the block.
The probability of cancelling $\ell$ surgeries is given by $P(X = n - \ell)$, for $\ell = 0, 1, \ldots, n-1$.
As an illustration, if $n = 5$ surgeries were planned in the block, the probability of cancelling $1$ surgery is given by the probability of performing $4$ surgeries (i.e., $P(X = 4)$).
Therefore, by varying $n$, we can obtain different cancellation probability distributions and analyse the impact of the number of surgeries planned in the block on the cancellation probabilities.

\textcolor{red}{Indeed, this session needs reshufling and better writing. All the elements are there, but it is too confusing as it is now!}

% ********************************************************************************* %
\subsection{Inventory-based model for defining the number of surgeries in a time block\label{sec:patientsperblock}}
% ********************************************************************************* %

Using the estimated probabilities of performing and cancelling surgeries, we now determine the number of procedures to be planned within a single operating theatre block. This decision is formulated through an inventory-based model that maximises the expected net value by balancing the benefits of performing surgeries against the costs associated with cancellations.

Let $G(k)$ denote the expected gain from planning $k$ surgeries within a block. This gain is derived from the surgery survival function, defined as $P(X > n)$, which represents the probability that at least $n+1$ surgeries can be accommodated within the available block time. Each successfully performed surgery yields a monetary value $R$, and the expected gain is therefore given by
\[
G(k) = R \cdot P(X > k-1),
\]
where $P(X > k-1)$ is the probability that at least $k$ surgeries are completed.

Planning additional surgeries increases the risk of cancellations, each of which incurs an expected cost. Let $C(k)$ denote the expected cancellation cost when $k$ surgeries are planned. If exactly $X=n$ surgeries are performed, then $k-n$ surgeries are cancelled. \textcolor{red}{In the last session, the number of planned surgeries was $n$. You need to keep notation consistent!} The cancellation cost is assumed to increase non-linearly with the number of cancelled surgeries, reflecting the compounded operational and clinical disruption associated with multiple cancellations. Specifically, the cost of cancelling $m$ surgeries is modelled as $c(m)=R\,m^{2}$. The expected cancellation cost can therefore be expressed as \textcolor{red}{You DO NOT need to specify $c(n)$. Just stating that this is a non-decreasing function of $n$ should suffice. The function you use in your experiments should be justified in the experiments. Bearing in mind that $n$ in the last session was the number or \underline{planned} surgeries.}
\[
C(k) = \mathbb{E}[c(k-X)] 
     = \sum_{n=0}^{k} P(X=n)\, c(k-n)
     = \sum_{n=0}^{k} P(X=n)\, R (k-n)^2,
\]
where $P(X=n)$ denotes the probability that exactly $n$ surgeries are performed.

The gain function $G(k)$ is non-increasing in $k$ (\textcolor{red}{But you have just specified it as a linear function before! Why?}, as marginal benefits diminish when additional surgeries are planned, whereas the cost function $C(k)$ is increasing due to the rising likelihood and severity of cancellations. Consequently, there exists a threshold beyond which the marginal cost of planning further surgeries exceeds the marginal gain.

Given the discrete nature of the decision variable, the expected net value \textcolor{red}{Net value of what?} is evaluated as $\Delta(k)=G(k)-C(k)$ for each feasible value of $k$. The optimal number of surgeries to be planned within a block is then defined as
\[
k^{*} = \max \left\{ k \in \mathbb{N} \;:\; \Delta(k) > 0 \right\},
\]
where $k^{*}$ depends on the surgery-duration distributions, total block time, time-budget threshold, unit revenue per surgery and cancellation cost structure.


For each subspecialty, we can define the 