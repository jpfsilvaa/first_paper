\documentclass[a4paper, twoside, 12pt]{report}
\usepackage{standalone}
\usepackage[a4paper,showframe,width=150mm,top=25mm,bottom=25mm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage{pgfplots}
\usepackage{fixltx2e}
\usepackage{siunitx}
\usepackage{subfig}
\usepackage{xcolor}

\usepackage[active,tightpage]{preview}
\setlength{\PreviewBorder}{0pt}
\usepackage{tikz}
\PreviewEnvironment[{[]}]{tikzpicture}
\begin{document}
\begin{tikzpicture}[
    font=\sffamily,
    >={Stealth[length=3mm]},
    line width=0.9pt,
    node distance=6mm and 8mm,
    
    input/.style={
        draw,
        fill=white,
        minimum width=2.6cm,
        minimum height=1.5cm,
        align=center
    },
    process/.style={
        draw,
        fill=blue!20,
        minimum width=3.2cm,
        minimum height=1.6cm,
        align=center,
        font=\bfseries
    },
    output/.style={
        draw,
        fill=green!20,
        minimum width=2.5cm,
        minimum height=1.5cm,
        align=center,
        font=\bfseries
    },
    bigbox/.style={
        draw=red!85!black,
        very thick,
        inner sep=10pt,
        rounded corners=1pt
    },
    every path/.style={draw, ->}
]

% =========================================================
% (i) Queue Management
% =========================================================

\node[font=\bfseries\itshape\Large, anchor=west] (title1) at (0,8.6) {(i) Queue Management};

\node[input, below=6mm of title1.west, anchor=west] (pdemand) {Patient\\demand\\probability\\distribution};
\node[input, below=2mm of pdemand] (waitreq) {Waiting time\\requirement};
\node[input, below=2mm of waitreq] (conf) {Confidence\\level};

\node[process, right=1.8cm of waitreq] (rqpolicy) {(R,Q) Control\\Policy};

\node[output, right=1.5cm of rqpolicy, yshift=1.1cm] (upperq) {Upper\\bound Q};
\node[output, right=1.5cm of rqpolicy, yshift=-1.1cm] (lowerq) {Lower\\bound Q};

\draw (pdemand.east) -- ++(0.7,0) |- (rqpolicy.west);
\draw (waitreq.east) -- (rqpolicy.west);
\draw (conf.east) -- ++(0.7,0) |- (rqpolicy.west);

\draw (rqpolicy.east) -- ++(0.45,0) |- (upperq.west);
\draw (rqpolicy.east) -- ++(0.45,0) |- (lowerq.west);

% Box around section (i)
\node[bigbox, fit=(title1) (pdemand) (waitreq) (conf) (rqpolicy) (upperq) (lowerq)] (box1) {};

% =========================================================
% Preprocessing
% =========================================================

\node[process, minimum width=3.4cm, right=7.2cm of rqpolicy] (prep) {Preprocessing};

% Connector from queue outputs to preprocessing
\coordinate[right=3.5cm of upperq.east] (mergeQ);
\draw (upperq.east) -- (mergeQ |- upperq.east) |- (prep.west);
\draw (lowerq.east) -- ++(3.2,0) |- (prep.west);

% =========================================================
% (ii) Overtime and Cancelation Management
% =========================================================

\node[font=\bfseries\itshape\Large, anchor=west] (title2) at (1.8,-0.2) {(ii) Overtime and Cancelation Management};

\node[input, below=10mm of title2.west, anchor=west] (sdur) {Surgery\\duration\\probability\\distribution};
\node[input, below=2mm of sdur] (ocost) {Overtime\\cost};
\node[input, below=2mm of ocost] (icost) {Idle time\\cost};
\node[input, below=2mm of icost] (bsize) {Block size};

\node[process, right=2.0cm of ocost] (newsvendor) {Newsvendor\\model};

\node[output, right=1.6cm of newsvendor, yshift=1.2cm, minimum width=3.0cm, minimum height=1.8cm] (pfit)
{Probabilities\\of surgeries\\to fit into the\\block};

\node[output, right=1.6cm of newsvendor, yshift=-1.2cm, minimum width=3.0cm, minimum height=1.8cm] (pcancel)
{Probabilities\\of canceling\\the surgery};

\node[process, right=2.2cm of pfit.south east, xshift=0.2cm, yshift=-1.25cm, minimum width=3.2cm, minimum height=1.8cm] (inventory)
{Inventory-\\based model};

\node[output, right=1.4cm of inventory, minimum width=2.7cm, minimum height=1.7cm] (nplan)
{Number of\\surgeries to\\plan in a\\block};

\draw (sdur.east) -- ++(0.8,0) |- (newsvendor.west);
\draw (ocost.east) -- ++(0.8,0) |- (newsvendor.west);
\draw (icost.east) -- ++(0.8,0) |- (newsvendor.west);
\draw (bsize.east) -- ++(0.8,0) |- (newsvendor.west);

\draw (newsvendor.east) -- ++(0.5,0) |- (pfit.west);
\draw (newsvendor.east) -- ++(0.5,0) |- (pcancel.west);

\draw (pfit.east) -- (inventory.west |- pfit.east);
\draw (pcancel.east) -- (inventory.west |- pcancel.east);

\draw (inventory.east) -- (nplan.west);

% Box around section (ii)
\node[bigbox, fit=(title2) (sdur) (ocost) (icost) (bsize) (newsvendor) (pfit) (pcancel) (inventory) (nplan)] (box2) {};

% =========================================================
% (iii) Operating Theatre Schedule
% =========================================================

\node[font=\bfseries\itshape\Large, anchor=west] (title3) at (19.3,0.0) {(iii) Operating Theatre Schedule};

\node[input, below=10mm of title3.west, anchor=west] (compat) {Theatre\\compatibility};
\node[input, below=2mm of compat] (teams) {Teams\\availability};
\node[input, below=2mm of teams] (anes) {Anaesthetists\\availability};

\node[process, right=1.8cm of teams, minimum width=3.2cm, minimum height=1.8cm] (milp) {MILP allocation\\model};

\node[output, right=1.4cm of milp, minimum width=2.7cm, minimum height=1.8cm] (msched) {Monthly\\Theatre\\Schedule};

\draw (compat.east) -- ++(0.8,0) |- (milp.west);
\draw (teams.east) -- (milp.west);
\draw (anes.east) -- ++(0.8,0) |- (milp.west);

\draw (milp.east) -- (msched.west);

% Box around section (iii)
\node[bigbox, fit=(title3) (compat) (teams) (anes) (milp) (msched)] (box3) {};

% =========================================================
% Cross-section connections
% =========================================================

% From number of surgeries to preprocessing
\draw (nplan.east) -- ++(2.2,0) |- ([yshift=-2mm]prep.west);

% From preprocessing down to section (iii)
\coordinate (downprep) at ($(prep.south)+(0,-1.7)$);
\draw (prep.south) -- (downprep) -| ([yshift=8mm]milp.west);

\end{tikzpicture}
\end{document}